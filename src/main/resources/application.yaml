spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mgrDB
    username: "mgrDB"
    password: "mgrDB"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true













  #jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect